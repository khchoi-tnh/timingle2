apiVersion: v1
kind: Secret
metadata:
  name: timingle-secrets
  namespace: timingle
type: Opaque
stringData:
  # NOTE: Replace these with actual secrets in production
  # Use external secret management (Vault, AWS Secrets Manager, etc.)
  POSTGRES_USER: "timingle"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION_USE_STRONG_SECRET"
